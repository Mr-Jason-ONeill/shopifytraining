const swiper = new Swiper('.swiper', {
  // Optional parameters
  direction: 'vertical',
  loop: true,
  autoplaySpeed: 2000

  // If we need pagination
  pagination: {
    el: '.swiper-pagination',
  }
});